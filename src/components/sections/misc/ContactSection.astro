---
// Import the necessary dependencies.
import AuthBtn from "@components/ui/buttons/AuthBtn.astro";
import ContactIconBlock from "@components/ui/blocks/ContactIconBlock.astro";
import TextInput from "@components/ui/forms/input/TextInput.astro";
import EmailContactInput from "@components/ui/forms/input/EmailContactInput.astro";
import PhoneInput from "@components/ui/forms/input/PhoneInput.astro";
import TextAreaInput from "@components/ui/forms/input/TextAreaInput.astro";
import Icon from "@components/ui/icons/Icon.astro";
import AvatarTestimonialSection from "@/components/ui/avatars/AvatarTestimonialSection.astro";
import AvatarAboutUs from "@/components/ui/avatars/AvatarAboutUs.astro";
import CardPresentation from "@/components/ui/cards/CardPresentation.astro";
import AboutUsSection from "./AboutUsSection.astro";

// Define the variables that will be used in this component
const title: string = "Agenda una cita aqui";
const subTitle: string =
  "Necesitas ayuda o asesoria en tu proyecto? Agenda una cita con un Maker.";
const formTitle: string = "Agenda una cita";
const formSubTitle: string = "We'll get back to you in 1-2 business days.";
---

<!-- Contact Us -->
<section
  id="contact"
  class="mx-auto max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14"
>
  <div class="mx-auto max-w-2xl lg:max-w-5xl">
    <div class="text-center">
      <h1
        class="text-balance text-2xl font-bold tracking-tight text-neutral-800 dark:text-neutral-200 md:text-4xl md:leading-tight"
      >
        {title}
      </h1>
      <p class="mt-1 text-pretty text-neutral-600 dark:text-neutral-400">
        {subTitle}
      </p>
    </div>

    <div class="mt-12 grid items-center gap-6 lg:grid-cols-2 lg:gap-16">
      <div class="flex flex-col rounded-xl p-4 sm:p-6 lg:p-8">
        <h2
          class="mb-8 text-xl font-bold text-neutral-700 dark:text-neutral-300"
        >
          {formTitle}
        </h2>
        <!-- Form for user input with various input fields.-->
        <!-- Each field utilizes a different input component for the specific type of input (text, email, phone, and textarea)-->
        <!-- <form>
          <div class="grid gap-4">
            <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
              <TextInput
                id="hs-firstname-contacts"
                label="First Name"
                name="hs-firstname-contacts"
              />
              <TextInput
                id="hs-lastname-contacts"
                label="Last Name"
                name="hs-firstname-contacts"
              />
            </div>
            <EmailContactInput id="hs-email-contacts" />
            <PhoneInput id="hs-phone-number" />
            <TextAreaInput
              id="hs-about-contacts"
              label="Details"
              name="hs-about-contacts"
            />
          </div>

          <div class="mt-4 grid">
            <AuthBtn title="Send Message" />
          </div>

          <div class="mt-3 text-center">
            <p class="text-sm text-neutral-600 dark:text-neutral-400">
              {formSubTitle}
            </p>
          </div>
        </form> -->
        <img src="src/images/calendlyqr.png" alt="QR code get an apointment" />
        <a
          type="submit"
          href="https://calendly.com/makergdl/asistencia-o-asesoria-en-maker-space"
          class={"inline-flex mt-3 w-full items-center justify-center gap-x-2 rounded-lg px-4 py-3 text-sm font-bold text-neutral-200 focus-visible:ring outline-none transition duration-300 border border-transparent bg-primary dark:focus:outline-none hover:bg-accent-500 2xl:text-base disabled:pointer-events-none disabled:opacity-50 ring-zinc-500 dark:ring-zinc-200"}
          >{title}</a
        >
      </div>

      <!--ContactIconBlocks are used to display different methods of contacting, including visiting office, email, browsing knowledgebase, and FAQ.-->
      <div class="divide-y divide-neutral-300 dark:divide-neutral-700">
        <ContactIconBlock
          heading="Documentacion"
          content="Explora las guias y tutoriales sobre el equipo del maker space."
          isLinkVisible={true}
          linkTitle="Ir a la documentacion"
          linkURL="/welcome-to-docs"
          isArrowVisible={true}
          ><Icon name="question" />
        </ContactIconBlock>

        <ContactIconBlock
          heading="Blog"
          content="Puedes encontrar ejemplos de proyectos realizados en el maker en nuestro blog."
          isLinkVisible={true}
          linkTitle="Ir al blog"
          linkURL="/blog"
          isArrowVisible={true}
          ><Icon name="chatBubble" />
        </ContactIconBlock>

        <ContactIconBlock
          heading="Ubicacion del Maker Space"
          content="3er piso del edificio de negocios del Tec de Monterrey campus GDA"
          isAddressVisible={true}
          addressContent="Edicificio que esta frente al Centro de congresos"
          ><Icon name="mapPin" />
        </ContactIconBlock>

        <!-- <ContactIconBlock
          heading="Contact us by email"
          content="Prefer the written word? Drop us an email at"
          isLinkVisible={true}
          linkTitle="support@screwfast.uk"
          linkURL="#"
          ><Icon name="envelopeOpen" />
        </ContactIconBlock> -->
      </div>
    </div>
  </div>
</section>

